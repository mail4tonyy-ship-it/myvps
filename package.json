{
  "name": "probe-panorama-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "保存探针节点、监控指标、登录会话和大盘设置的 D1 数据库。"
      },
      "ASSETS": {
        "description": "托管前端页面和 VPS Agent 安装脚本。"
      },
      "ADMIN_USERNAME": {
        "description": "管理员用户名，默认 admin。"
      },
      "ADMIN_PASSWORD": {
        "description": "管理员密码。部署后请立即改成强密码。"
      },
      "DASHBOARD_HUB": {
        "description": "用于公开大盘实时推送的 Durable Object。"
      },
      "VPS_PRESENCE": {
        "description": "兼容原 K-UI 部署形态保留的 Durable Object 绑定。"
      }
    }
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
